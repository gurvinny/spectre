[project]
name = "spectre-sensor"
version = "0.1.0"
description = "SPECTRE — WiFi threat sensor: UART ingest, detection, API, Wazuh forwarding"
authors = [{ name = "gurvinny" }]
license = { text = "AGPL-3.0-or-later" }
requires-python = ">=3.11"
dependencies = ["fastapi>=0.110,<1.0", "uvicorn[standard]>=0.29,<1.0"]

[tool.setuptools.packages.find]
include = ["spectre*"]
